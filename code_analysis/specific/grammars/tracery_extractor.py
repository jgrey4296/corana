import json
import networkx as nx
import re

KEY_REG = re.compile("(#.+?)#")

if __name__ == '__main__':
    # load file

    # Get keys

    # for each entry, get edges

    # create graph

    # save graph
